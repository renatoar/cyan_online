package main

object Program
    func run {
        var Person p = Person("Ana", 6);
        p getName println;
        
        var b = Box<Int>(5);
        Out println: b getValue;
        let Pair<Int, Char> pic = Pair<Int, Char>(48, '0');
        assert pic getFirst == 48;
        
        // uncomment to see the compilation errors
        // var Pair<Person, Int> pp;
        // var Pair<Int, Person> pp2;
    }
end
