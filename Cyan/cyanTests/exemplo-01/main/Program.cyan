package main
import graph

object Program
    func run {
        var g = Graph();
        g add:
          edge: 1, 4
          edge: 3, 1
          edge: 1, 2
          edge: 2, 4;
               
        g println;
        assert g asString == "{ (1, 4), (3, 1), (1, 2), (2, 4) }";

        g add: Edge(1, 3);  
        
        printexpr g;  
    }
end
