package main

@concept{*
     T has [ 
         func search: typeof(R get) -> typeof(T get: Int)
         func at: Int -> typeof(R at: Int)     
         func get: Int -> Double
         ],
     R has [ 
        func get -> Program
        func at: Int n -> typeof(T at: 0)
     ],
     cyan.lang.lessThan(T),
     typeof(R at: Int) in [ Int, Long, typeof(Int asString) ]
     
*}
object Strange<T, R>
end
